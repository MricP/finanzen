{{ form_start(form, {'attr': {'class': 'new-article-form'}}, {action: path("app_article_new")}) }}
    {{ form_widget(form) }}
<button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}