<form method="post" action="{{ path('app_magasin_delete', {'id': magasin.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ magasin.id) }}">
    <button class="delete-button-admin" class="btn">Supprimer la catégorie</button>
</form>
