{% extends 'base.html.twig' %}

{% block title %}Edit Liste{% endblock %}

{% block body %}
 <style>
    .example-wrapper { background-color: #F3F3F3; border: 2px solid rgba(36, 40, 97, .2) ; padding: 25px; margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
</style>
<div class="example-wrapper">
    <a class="return-to-list-link" href="#" onclick="window.history.back(); return false;">Retourner à la page précédente.</a>
    <h1 class="title-admin">
        Modifier la liste 
        <img class="title-admin-icon" src="{{ asset('images/edit-icon.jpg') }}" alt="crayon sous forme d'icon"/>
    </h1>

    {{ include('liste/_form_edit.html.twig', {'button_label': 'Modifier'}) }}
</div>
{% endblock %}
