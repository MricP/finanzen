{{ form_start(form, {action: path("app_liste_edit", {id: liste.id}), attr: {id: 'edit-list-form'}}) }}
    {{ form_widget(form) }}
<button class="btn">{{ button_label|default('Modifier') }}</button>
{{ form_end(form) }}